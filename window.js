const name = "kuddus"
function add(num1, num2) {
    const result = num1 + num2;
    return result;
    function double(num) {
        return num + 2;
    }
    const sum = double(20)
    return sum;
}

const total = add(10, 20);
console.log(total);
// console.log(result);